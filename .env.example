# Set to the deployed GrantGuardProtocol address on Studionet.
# When empty, the UI runs against the bundled mock data so the demo
# remains end-to-end without a deployment.
NEXT_PUBLIC_GRANTGUARD_CONTRACT=
NEXT_PUBLIC_GENLAYER_RPC=https://studio.genlayer.com:8443/api
